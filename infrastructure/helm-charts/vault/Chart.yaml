apiVersion: v2
name: vault
description: Local wrapper chart for HashiCorp Vault
type: application
version: 0.1.0
appVersion: "1.19.0"

dependencies:
  - name: vault
    version: "0.31.0"
    repository: "https://helm.releases.hashicorp.com"
